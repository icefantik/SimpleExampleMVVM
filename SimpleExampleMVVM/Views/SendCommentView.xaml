<Window x:Class="SimpleExampleMVVM.Views.SendCommentView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SimpleExampleMVVM.Views"
        mc:Ignorable="d"
        Title="SendCommentView" Height="450" Width="800">
    
        <StackPanel DataContext="{Binding SendCommentViewModel}">
            <!-- DataContext="{Binding SendCommentViewModel}" -->
            <TextBox Text="{Binding Comment}"/>

            <Button Command="{Binding SendComment}">Send</Button>
        </StackPanel>
    
</Window>
